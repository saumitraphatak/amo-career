# Navigation / Information-Architecture Re-Audit — September 23, 2026

Scope: same ground as the original `docs/navigation-audit-2026-09.md` (global nav dropdown/mobile overlay, global search index, footer links, "See Also" cross-linking, in-page anchor/section-nav integrity, click-depth/discoverability from `home.html`, Guided Paths, breadcrumbs) — re-checked because this was the item that had gone longest without a re-check (last done 2026-09-09, 14 days), and because 3 new pages (`quantinuum-vs-ionq.html`, `google-vs-ibm.html`, `psiquantum-vs-xanadu.html`) were added 2026-09-22, after that original audit, without a dedicated IA pass verifying their site-wide integration held up under the checks this audit runs. Did not re-open the three items the 2026-09 audit already flagged as judgment calls (tools-grid coverage, guided-path coverage, visible breadcrumbs) beyond confirming whether the new pages change their shape.

Method: read `js/main.js`'s `NAV`/`renderNav`/`initRelatedToolsPanel`/`SEARCH_KEYWORDS` directly rather than delegating to a subagent this round — the surface area to check was narrow (3 specific new pages against the site's existing IA conventions, plus a general sitewide consistency sweep) and fit comfortably in-context. Verified every claim against the actual files (grep counts, not description). Regression suite (11/11) kept green throughout; tag-balance checked on every file touched (`home.html`, `404.html`).

## Fixed

- **The 3 new comparison pages had no `.footer-link` entry anywhere on `home.html`.** Their own page-notes describe them as added to `NAV.career`, `SEARCH_KEYWORDS`, `SOURCE_PROFILES`, `PAGE_PLAYBOOKS`, `RELATED_TOOLS`, and `REFERENCE_TRAILS` on creation (all 6 verified present and correct) — but the footer, which every one of the other 28 pages has exactly one entry in, was missed. This isn't a cosmetic gap: it's the same class of bug the 2026-09 audit fixed for `zernike.html`/`cavity-qed.html` (present in `NAV` + dropdown + tools-grid but absent from footer). Added `quantinuum-vs-ionq`, `google-vs-ibm`, and `psiquantum-vs-xanadu` to the "Learn & Career" footer column, next to their `rb87-vs-yb171` sibling. Verified afterward: 31 `.footer-link` entries on `home.html`, one per page, zero duplicates. Updated `CLAUDE.md`'s footer-column documentation ("all 4 `career` tools" → "all 7") so it doesn't undercount again.
- **`404.html` was still requesting `js/main.js?v=24`** while every other one of the site's 32 other HTML files was on `v=26` — missed in both of the last two cache-bust bumps (the `google-vs-ibm`/`psiquantum-vs-xanadu` commit bumped v25→v26 across "32 files," and `404.html` wasn't one of them; it was also missed at v24→v25 for `quantinuum-vs-ionq`). `styles.css` was correctly at `v=21` everywhere including `404.html`, so only the JS reference had drifted. This isn't just a stale-cache nitpick — since `main.js` has changed twice since `404.html` was last bumped, a visitor's browser could genuinely be running an outdated `main.js` (different `NAV`, different search index, etc.) on the error page specifically. Bumped `404.html` to `v=26`; grep-verified all 33 files now agree.

## Flagged, not changed — needs your call

- **`llms.txt` and `llms-full.txt` have zero mentions of any of the 3 new comparison pages.** Every other new page in this site's history (per `site-philosophy.md`'s own convention) gets added to both files as part of standard site integration, but the page-notes for all 3 new pages list `js/main.js`, `sitemap.xml`, and manual `.see-also` links as what was touched — `llms.txt`/`llms-full.txt` aren't mentioned, and grepping confirms they're genuinely absent. I'm treating this as technical/SEO-audit territory rather than fixing it here, matching the boundary the 2026-09 nav audit itself drew ("sitemap/meta-tag/page-count staleness is the technical audit's job") — `llms.txt` is the same category of machine-readable index as the sitemap, just AI-crawler-facing instead of search-engine-facing. Flagging clearly so it isn't lost: this should be the top item whenever the technical/SEO audit is next redone (last done 2026-09-16, before these pages existed).
- **Tools-grid and Guided-Paths coverage gaps grew by 3, same pattern as before.** The 2026-09 audit found 3-of-28 tools with no `tools-grid` card (`laser-cooling`, `learn-quantum`, `rb87-vs-yb171` — all comparison/overview/learning-path pages, not single-purpose calculators) and 7-of-28 in no Guided Path. The 3 new pages are also comparison pages and fit neither the grid nor any existing path for the same structural reason, so the counts are now 6-of-31 and 10-of-31 respectively — but this isn't a new problem, it's the same already-flagged editorial judgment call (whether comparison pages belong in a grid of tool *cards*, and whether/how to route a 4th of the catalog through a "Where should you start?" path) simply applying to more pages now. Not re-opening it past noting the updated counts, per the original audit's own framing.
- **Visible breadcrumbs**: still none, same as 2026-09's finding (only invisible `BreadcrumbList` JSON-LD, confirmed present and correctly structured on all 3 new pages — `Home › Career › <page>`). No new evidence changes the original call that this is a possible future design item, not a mechanical fix.

## Confirmed clean (no action)

- **Desktop nav dropdown + mobile overlay**: both are rendered entirely from `NAV.career` at runtime (`renderNav()` in `js/main.js`), so all 3 new pages appear correctly and automatically in both, with the mobile `kind`-subtitle fix from the 2026-09 audit applying to them for free. No hardcoded/duplicated nav markup found anywhere that could drift out of sync with `NAV`.
- **In-page section navigation on the 3 new pages**: these pages use `data-target="..."` buttons + JS scroll (not `href="#id"` anchors like older pages) for their sticky topic-nav — checked all 3 pages' `data-target` values against their section `id`s directly; every one resolves, zero dangling targets.
- **"See Also" duplicate-panel guard** (the auto-injection bug the 2026-09 audit fixed by broadening the guard to `document.querySelector('.see-also')`): all 3 new pages ship hand-curated `.see-also` sections, so the guard correctly suppresses the auto-generated panel on all of them — spot-checked the rendered markup, no double panels.
- **`sitemap.xml`**: all 3 new pages present as `<url>` entries, no duplicates.
- **Search keyword coverage**: all 3 new pages have non-trivial `SEARCH_KEYWORDS` entries (checked they're not title-only fallbacks like the 6-page gap the 2026-09 audit found and fixed).
- **Hero tool-count stat** (`#stat-tools-count` on `home.html`): computed live from `NAV.tools.length` at runtime, so it already correctly reads 31 despite the static HTML fallback saying "28" — not a bug, self-healing by design.
- **Click depth**: all 3 new pages are now reachable from `home.html` in exactly 1 click (nav dropdown, footer link post-fix) — previously they were reachable but only via `.see-also` cross-links from `qc-landscape.html`/each other (2 clicks) or search.

## Not covered this round

- The tools-grid/guided-path coverage questions and visible-breadcrumb item above remain open editorial calls from the original audit, now touching a slightly larger set of pages — still yours to decide, not re-litigated here.
- `llms.txt`/`llms-full.txt` staleness flagged above but left for the technical/SEO audit rather than fixed in this pass, to keep this audit's own scope from creeping into a different one's territory.
